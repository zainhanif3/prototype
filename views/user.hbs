

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DRC Users</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    />
  </head>
  <body class="bg-gray-100 p-4">

    <h1 class="text-3xl font-bold mb-4">DRC Users</h1>

    <form action="/add-user" method="POST" class="mb-4">
      <label class="block mb-2">Name:</label>
      <input type="text" name="name" class="border p-2 mb-2" required />

      <label class="block mb-2">Position:</label>
      <input type="text" name="position" class="border p-2 mb-4" required />

      <button type="submit" class="bg-blue-500 text-white py-2 px-4 rounded">
        Add User
      </button>
    </form>

    <!-- Search Form -->
    <form action="/search" method="get" class="mb-4 p-5">
      <label for="searchTerm">Search:</label>
      <input type="text" name="searchTerm" required class="mr-2" />
      <select name="searchType" class="mr-2">
        <option value="name">Name</option>
        <option value="email">Email</option>
        <option value="cnic">Cnic</option>
        <option value="number">Number</option>
      </select>
      <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded">
        Search
      </button>
    </form>
    {{!-- data fetch update and delete --}}
    <ul class="mb-4 p-5">
      {{#each users}}
        <li class="mb-5 p-5 gap-2">
          {{name}} - {{email}} - {{password}} - {{cnic}} - {{number}}
          <a href="/add-user/{{_id}}/update" class="text-blue-500 ml-4 bg-gray-50 border-l-4 text-2xl">
            Update
          </a>
          <a href="/add-user/{{_id}}/delete" class="text-red-500 ml-4 bg-gray-50 border-l-4 text-2xl">
            Delete
          </a>
        </li>
      {{/each}}
    </ul>

  </body>
</html>
